# Gaze - NatHacks 2025
A way to use your computer hands-free through EOG processing
## Requirements
Install and set up the latest version of [Docker](https://docs.docker.com/get-started/get-docker/).

Clone the repository onto your machine (`git clone https://github.com/arronroasa/gaze-nathacks2025.git`).

This project requires some Python packages as prerequisites.
> If Docker is set up correctly, they should be installed in the docker environment when you execute `run.sh`.
```
contourpy==1.3.3
cycler==0.12.1
fonttools==4.60.1
kiwisolver==1.4.9
matplotlib==3.10.7
MouseInfo==0.1.3
numpy==2.3.4
packaging==25.0
pandas==2.3.3
pillow==12.0.0
PyAutoGUI==0.9.54
PyGetWindow==0.0.9
PyMsgBox==2.0.1
pyparsing==3.2.5
pyperclip==1.11.0
PyRect==0.2.0
PyScreeze==1.0.1
pyserial==3.5
python-dateutil==2.9.0.post0
pytweening==1.2.0
pytz==2025.2
six==1.17.0
tzdata==2025.2
```
## Usage
Once you have cloned the project onto your local machine, open a terminal in the repository folder and run:

```bash
bash ./run.sh
```

This should automatically install the dependencies and run the `test.py` file.
## Features
WIP
